# The ID of your GitHub App
APP_ID = (your app id goes here)
WEBHOOK_SECRET = (the webhook secret goes here)

# Uncomment this to get verbose logging; use `info` to show less
#LOG_LEVEL=trace

# Go to https://smee.io/new set this to the URL that you are redirected to.
WEBHOOK_PROXY_URL = (the webhook url goes here)
PRIVATE_KEY_PATH = (the path to the private key)
